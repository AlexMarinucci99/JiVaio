/// Rappresenta una fermata del trasporto urbano visualizzabile sulla mappa.
class TransitStop {
  const TransitStop({required this.latitude, required this.longitude});

  final double latitude;
  final double longitude;
}
